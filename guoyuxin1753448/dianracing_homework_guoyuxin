填空题：
1、cin, ifstream, istringstream, cout, ofstream, ostringstream
2、break 不执行本次循环后的语句，并且跳出循环，不再执行。
   continue 不执行本次循环后面的语句，直接进入下一次循环。
3、1 2 4 4 8 4 8
4、值传递 指针传递 引用传递
5、public, protected, private

简答题：
1、我认为面向对象编程就是把待解决的问题分解成各个对象，描述各个事物在解决问题中的过程中的步骤和行为。将事物一些共有的属性和行为抽象成类，通过实例化创建多个对象，再将对应的程序和数据封装在里面。提高了程序的可读性，重用性，可扩展性。
2、声明：仅将变量告诉编译器，不一定为变量分配内存。
   定义：给变量分配内存空间。
   初始化：在定义时给变量一个初始值。
3、char*定义了一个字符串指针。
   char[]定义了一个字符串数组。
   string定义了一个字符串类的对象。
4、.是成员运算访问符，使用时左侧是结构变量名。
   ->是结构指向运算符，使用时左侧是指向结构的指针。


